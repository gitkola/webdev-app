{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start:cli": "lerna run start --scope webdev-cli",
    "start:ui": "lerna run start --scope webdev-ui"
  },
  "devDependencies": {
    "lerna": "^8.1.3"
  }
}
